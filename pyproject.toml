[project]
name = "m4ml-2025-normalizing-flows"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pip>=25.1.1",
    "torch>=2.7.1",
]
